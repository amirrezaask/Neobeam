* Diff viewer as a seperate part of the application. ?
* Integrate winbar into the menubar so first we have the settings option which can become a simple settings emoji and then we will have the winbar( file name , project, and maybe git or lsp status )
* This program should act also as a translation layer for neovim to bind keys that are unmappable in neovim. ( cmd in macos for eg )
?* Maybe we can just ship a whole configuration in this ? ( the program will set neovim up itself instead of using the user's configuration)
* Integrate dev tools here.
